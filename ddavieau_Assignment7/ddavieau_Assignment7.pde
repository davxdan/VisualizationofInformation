//Instructions Use OOP to re-create a past project
void setup(){
  size(3840, 2160);
  
  //gridMaker: creates a grid along x and y axis. It auto centers in the sketch.
  //Arguments:(int gridWidth, int gridHeight,int cols, int rows,color strokeColor, color fillCol)
  gridMaker(1920, 1080,40, 40,color(245,128,0), 225); 
  
  
  //dataLoader: loads 2 dimensional data from a .csv file into an array
  filename, header,
  
  }
